# fesalfayed.com

Personal site. Static HTML + Tailwind (CDN). Deployed via Cloudflare Pages.

## Deploy
Push to `main` → Cloudflare Pages auto-builds & ships. No build step (pure static).

## Local preview
```
python3 -m http.server 8000
# open http://localhost:8000
```
